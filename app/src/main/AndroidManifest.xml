<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beginningandroid" >

    <application
        android:name="com.beginningandroid.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.BaseActivity"
            android:label="@string/title_activity_base" >
        </activity>
        <activity
            android:name=".Activities.HandlerActivity"
            android:label="@string/title_activity_handler" >
        </activity>
        <activity
            android:name=".Broadcasts.BroadcastActivity"
            android:label="BroadcastActivity" >
        </activity>
        <activity
            android:name=".Broadcasts.LoginActivity"
            android:label="LoginActivity" >
        </activity>
        <activity
            android:name=".Services.ServicesActivity"
            android:label="ServicesActivity" >
        </activity>
        <activity
            android:name=".Activities.LayoutActivity"
            android:label="LayoutActivity" >
        </activity>
        <activity
            android:name=".Activities.ListViewActivity"
            android:label="ListViewActivity" />
        <activity
            android:name=".Activities.Handler2Activity"
            android:label="@string/title_activity_handler2" >
        </activity>

        <service
            android:name=".Services.MyService"
            android:enabled="true"
            android:exported="true" >
        </service>
        <service android:name=".Services.MyIntentService"
            android:enabled="true"
            android:exported="true">

        </service>

        <service android:name=".Services.AlarmService"
            android:enabled="true"
            android:exported="true">

            </service>
        <receiver android:name=".Services.AlarmReceiver"></receiver>
    </application>

</manifest>
